# Garage EC2 deployment environment
# Copy to .env and fill in the secrets

# Managed Postgres (RDS)
# Example: postgresql+psycopg2://<user>:<password>@db-deploy.cano2kka0hmi.us-east-1.rds.amazonaws.com:5432/db-deploy?sslmode=require
DATABASE_URL=

# Shared JWT secret (gateway + auth)
JWT_SECRET=

# Frontend -> gateway proxy
VITE_PROXY_TARGET=http://gateway:8080
