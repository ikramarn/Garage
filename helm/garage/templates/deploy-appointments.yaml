{{- if .Values.components.appointments.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: garage-appointments
  labels:
    {{- include "garage.labels" (dict "name" "appointments" "Release" .Release) | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: garage-appointments
  template:
    metadata:
      labels:
        app: garage-appointments
        {{- include "garage.labels" (dict "name" "appointments" "Release" .Release) | nindent 8 }}
    spec:
      containers:
        - name: appointments
          image: {{ include "garage.image" (dict "Values" .Values "name" "appointments") }}
          imagePullPolicy: IfNotPresent
          env:
            - name: DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ include "garage.fullname" . | default (printf "%s-env" .Release.Name) }}
                  key: DATABASE_URL
          ports:
            - name: http
              containerPort: 8000
          resources: {{- toYaml .Values.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: appointments
  labels:
    {{- include "garage.labels" (dict "name" "appointments" "Release" .Release) | nindent 4 }}
spec:
  selector:
    app: garage-appointments
  ports:
    - name: http
      port: 8000
      targetPort: 8000
{{- end }}