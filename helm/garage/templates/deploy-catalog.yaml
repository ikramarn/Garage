{{- if .Values.components.catalog.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: garage-catalog
  labels:
    {{- include "garage.labels" (dict "name" "catalog" "Release" .Release) | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: garage-catalog
  template:
    metadata:
      labels:
        app: garage-catalog
        {{- include "garage.labels" (dict "name" "catalog" "Release" .Release) | nindent 8 }}
    spec:
      containers:
        - name: catalog
          image: {{ include "garage.image" (dict "Values" .Values "name" "catalog") }}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          resources: {{- toYaml .Values.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: catalog
  labels:
    {{- include "garage.labels" (dict "name" "catalog" "Release" .Release) | nindent 4 }}
spec:
  selector:
    app: garage-catalog
  ports:
    - name: http
      port: 8000
      targetPort: 8000
{{- end }}